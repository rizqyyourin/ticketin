# Ticketin Feature Modules

Implementation order:
1. `auth`
2. `contact`
3. `queue`
4. `service-request`
5. `dashboard`
6. `rbac`

Each module should keep UI components, validation schemas, and service logic inside its own folder.
